import React from 'react';

const ExampleComponent = () => {
    return (
        <div>
            <h1>Example Component</h1>
            <p>This is an example component that can be reused in different parts of the application.</p>
        </div>
    );
};

export default ExampleComponent;